"""
MCP tool modules for claude-mcp-starter.

Each module exports:
    *_TOOLS    — list[Tool]  — Tool definitions surfaced to the MCP client
    *_HANDLERS — dict[str, Callable]  — name → async handler function
"""
